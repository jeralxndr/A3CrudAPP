<!--NavBar-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Smart Budgeter</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <% if(title=="Home") { %>
              <a class="nav-link active" href="/home">
            <% } else {%>
              <a class="nav-link" href="/home">
            <% } %>  
            <i class="fa-solid fa-house-chimney-user"></i> Home</a>
          </li>
          
          
          <li class="nav-item"></li>
            <% if(title=="About us") { %>
              <a class="nav-link active" href="/aboutus">
            <% } else {%>  
              <a class="nav-link" href="/aboutus">
            <% } %> 
            <i class="fa-solid fa-circle-info"></i> About us</a>
          </li>
          
          <li class="nav-item"></li>
            <% if(title=="Books") { %>
              <a class="nav-link active" href="/bookslist">
            <% } else {%>
              <a class="nav-link" href="/bookslist">
            <% } %>  
            <i class="fa-solid fa-wallet"></i> Transactions</a>
          </li>
          
          <li class="nav-item"></li>
          <% if(title=="Contact Us") { %>
            <a class="nav-link active" href="/contactus">
          <% } else {%>  
            <a class="nav-link" href="/contactus">
          <% } %> 
          <i class="fa-solid fa-address-card"></i> ContactUs</a>
          </li>

          

        </ul>
      </div>
    </div>
  </nav>
